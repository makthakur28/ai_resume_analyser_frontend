import 'package:flutter/material.dart';

class AppColors {
  // Premium minimalistic palette (Linear/Notion inspired)
  static const primary = Color(0xFF1A1A1A); // Deep slate
  static const background = Color(0xFFFBFBFB); // Ultra light gray
  static const surface = Colors.white;
  
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  
  static const accent = Color(0xFF0066FF); // Electric premium blue
  static const accentLight = Color(0xFFEBF3FF); 
  
  static const border = Color(0xFFE5E7EB);
  
  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);
  
  static const shadow = Color(0x0A000000);
}
